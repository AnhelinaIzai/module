<%= form_for @product, url: products_path(@product.id) do |f| %>

  <%= f.text_field :title %><br />
  <%= f.text_area :description, size: "60x12" %><br />
  <%= f.text_field :price %><br />
  <%= f.submit "Create" %>
<% end %>